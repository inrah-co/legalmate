export class DataEntryController {
  async setData(req: Request, res: Response) {
    // Logic to set data entry
  }

  async getAllData(req: Request, res: Response) {
    // Logic to get all data entries
  }
}
